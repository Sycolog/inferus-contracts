{
  "name": "inferus-tryouts",
  "license": "UNLICENSED",
  "scripts": {
    "codegen": "graph codegen",
    "build": "graph build",
    "deploy": "graph deploy --node https://api.studio.thegraph.com/deploy/ inferus-tryouts",
    "create-local": "graph create --node http://localhost:8020/ inferus-tryouts",
    "remove-local": "graph remove --node http://localhost:8020/ inferus-tryouts",
    "deploy-local": "graph deploy --node http://localhost:8020/ --ipfs http://localhost:5001 inferus-tryouts"
  },
  "dependencies": {
    "@graphprotocol/graph-cli": "^0.30.0",
    "@graphprotocol/graph-ts": "^0.27.0"
  },
  "version": "0.0.7"
}
